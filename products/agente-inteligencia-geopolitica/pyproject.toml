[project]
name = "agente-inteligencia-geopolitica"
version = "0.1.0"
description = "Agente de inteligencia geopolítica aplicada a comercio exterior y expansion internacional."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --basetemp=.pytest-tmp"
