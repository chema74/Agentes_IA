[project]
name = "agente-logistica-autonoma"
version = "0.1.0"
description = "Agente de continuidad logistica con discovery A2A, negociacion y circuit breaker."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --basetemp=.pytest-tmp"
