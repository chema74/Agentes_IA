[project]
name = "apollo-policy-enforcer-agent"
version = "0.1.0"
description = "Compuerta normativa neuro-simbolica para enforcement determinista de politicas corporativas."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --basetemp=.pytest-tmp"
