[project]
name = "asistente-legal-supervisado"
version = "0.1.0"
description = "Agente de revision contractual, redlining y negociacion gobernada por playbooks."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --basetemp=.pytest-tmp"
