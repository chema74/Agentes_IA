[project]
name = "orquestador-integridad-educativa"
version = "0.1.0"
description = "Orquestador multiagente para integridad del aprendizaje y supervision docente."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --basetemp=.pytest-tmp"
